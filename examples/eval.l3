let a = 8 / 2 + 20
a += 10
let b = a * 2

println(a)
println(b)

__trigger_gc()

fn factorial(a, n)
  if n == 0 then
    return 1
  end

  return n * factorial(a, n - 1)
end

fn main()
  println(factorial("a", 20))
end

main()

a = 0
if a then println("a is truthy")
elif b then println("b is truthy")
else println("neither a or b is truthy")
end
