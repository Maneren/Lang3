#!/bin/env lang3

let mut a = 8 / 2 + 20
a += 10
let b = a * 2

println(a)
println(b)

let first, second = [1, 2]

println(first)
println(second)

__trigger_gc()

fn factorial(n)
  if n == 0 then
    return 1
  end

  return n * factorial(n - 1)
end

fn main()
  println(factorial(20))
end

main()

a = 0
if a then println("a is truthy")
elif b then println("b is truthy")
else println("neither a or b is truthy")
end

let mut i = 0
while i < 10 do
  println(random(100))
  i += 1
end
