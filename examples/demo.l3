# This is a comment
let a = 1 + 2 + 2 * 3
let b = a * -3

print(a + b)

function add(a, b)
  return a + b
end

let c = add(a, b)

c += 1

let valid = 0 <= c and c < 10 or not (b == 0 and a == 0)

valid = not valid
